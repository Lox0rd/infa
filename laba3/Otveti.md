## Ответы на вопросы к лабе3
# //------------------------------------------------------- 0
# //------------------------------------------------------- 1
```bash
int F1(int n) {
    if (n==1) return 1;
    return (n * F1(n-1)); 
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F1</i> вычисляет факториал числа рекурсивно. Она продолжает вызывать себя, уменьшая значение `n`, пока `n` не станет равным 1, после чего возвращает результат умножения всех значений от `n` до 1.
</div>

# //------------------------------------------------------- 2
```bash
int F2(int A[],int a, int b) { 
    if (a>b) return 0; 
    if (a==b) return A[a];
    int m=(a+b)/2;
    int v1=F2(A,a,m),v2=F2(A,m+1,b);
    return v1>v2 ? v1: v2; 
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F2</i> находит максимальный элемент в подмассиве массива `A` с индексами от `a` до `b` включительно, используя рекурсию и деля массив на две части. Функция возвращает максимальное значение среди двух подмассивов.
</div>

# //------------------------------------------------------- 3
```bash
int F3(int A[],int a, int b) {
    if (a>b) return 0; 
    if (a==b) return A[a];
    int m=(a+b)/2;
    return F3(A,a,m)+F3(A,m+1,b); 
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F3</i> рекурсивно вычисляет сумму элементов массива `A` от индекса `a` до индекса `b` включительно, разделяя массив на две части и суммируя результаты.
</div>

# //------------------------------------------------------- 4
```bash
void F4(int A[], int a, int b) {
    int i,j,mode;
    if (a>=b) return;
    for (i=a, j=b, mode=1; i != j; mode >0 ? i++ : j--)
    if (A[i] > A[j]) {
        int c;
        c=A[i]; A[i]=A[j]; A[j]=c; mode = -mode;
    }
    F4(A,a,i-1); F4(A,i+1,b); 
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F4</i> реализует алгоритм быстрой сортировки для массива `A`. В данном случае используется разделение массива на части, которые затем сортируются рекурсивно, с операцией обмена элементов.
</div>

# //------------------------------------------------------- 5
```bash
int F5(int A[], int a, int b, int val) {
    int i,j,mode;
    if (a>=b) return -1;
    int m=(a+b)/2;
    if (val==A[m]) return m;
    if (val<A[m]) return F5(A,a,m-1,val);
    return F5(A,m+1,b,val);
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F5</i> выполняет бинарный поиск в отсортированном массиве `A` от индекса `a` до индекса `b`, чтобы найти индекс элемента, равного значению `val`. Если элемент найден, возвращается его индекс, иначе —1.
</div>

# //------------------------------------------------------- 6
```bash
long F6(int n) {
    if (n==0 || n==1) return 1;
    return F6(n-1)+F6(n-2);
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F6</i> вычисляет числа Фибоначчи рекурсивно, где `F6(n)` возвращает n-е число Фибоначчи. Базовые случаи: если `n` равно 0 или 1, возвращается 1.
</div>

# //------------------------------------------------------- 7
```bash
char *F7(char *p, char *s) {
    if ( *s =='\0') return p;
    *p++ = *s;
    p=F7(p, s+1);
    *p++ = *s;
    return p;
}
void z7() { char *q, S[80]; *F7(S, "abcd")=0; }
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F7</i> выполняет рекурсивное копирование строки `s` в строку `p`, при этом добавляя каждый символ дважды. Рекурсия продолжается до тех пор, пока не будет достигнут нулевой символ строки.
</div>

# //------------------------------------------------------- 8
```bash
void F8(char *&p, char *s) {
    if ( *s =='\0') return;
    *p++ = *s;
    F8(p, s+1);
    *p++ = *s;
}
void z8() {char *q, S[80]; q = S; F8(q,"abcd"); *q=0; 
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F8</i> рекурсивно копирует строку `s` в строку `p`, а затем снова добавляет эти же символы, таким образом, создавая строку, в которой каждый символ из `s` встречается дважды.
</div>

# //------------------------------------------------------- 9
```bash
double F9(double *pk, double x, int n) {
    if (n==0) return(*pk);
    return *pk + x *F9(pk+1,x,n-1);
}
void z9() {double B[]={5.,0.7,4.,3.} ,X=3.,Y=F9(B,X,4); }
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F9</i> рекурсивно вычисляет сумму элементов массива с коэффициентами, используя заданное значение `x` и число шагов `n`. Рекурсия продолжается до тех пор, пока не будет обработан весь массив.
</div>

# //------------------------------------------------------- 10
```bash
void F10(int *p, int nn) {
    if (nn==1) {
        *p=0; return;
    }
    for (int i=2; nn % i !=0; i++);
    *p=i; F10(p+1,nn/i);
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F10</i> разлагает число `nn` на простые множители и сохраняет их в массив `p`. Функция рекурсивно делит число на простые делители, пока оно не станет равным 1.
</div>