## Ответы на вопросы к лабе3
# //------------------------------------------------------- 0
```bash
struct ltree{
    int val;
    ltree *child,*next; };
struct tree{
    int val;
    tree *ch[4]; };
struct tree1{
    int val;
    int n;
    tree1 *ch[10]; };
struct btree{
    int val;
    btree *l,*r; };
```

<div>
  <b>Что делает код:</b><br>
  Структуры данных для различных типов деревьев, таких как: общее дерево с указателями на детей и следующий элемент, дерево с фиксированным числом детей, бинарное дерево, а также бинарное дерево с динамическим количеством детей.
</div>

# //------------------------------------------------------- 1
```bash
int F1(ltree *p) {
    int n=1;
    for (ltree *q=p->child; q!=NULL; q=q->next)
    n+=F1(q);
    return n;
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F1</i> рекурсивно подсчитывает количество узлов в дереве с произвольным количеством детей. Для каждого дочернего узла вызывается функция <i>F1</i>, и результат суммируется.
</div>

# //------------------------------------------------------- 2
```bash
int F2(ltree *p) {
    int n=p->val;
    for (ltree *q=p->child; q!=NULL; q=q->next)
    n+=F2(q);
    return n;
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F2</i> рекурсивно суммирует значения всех узлов в дереве, включая значения дочерних узлов.
</div>

# //------------------------------------------------------- 3
```bash
int F3(ltree *p) {
    int n=p->val;
    for (ltree *q=p->child; q!=NULL; q=q->next) {
        int vv=F3(q);
        if (vv > n) n=vv;
    }
    return n;
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F3</i> рекурсивно находит максимальное значение среди всех узлов в дереве, включая значения дочерних узлов.
</div>

# //------------------------------------------------------- 4
```bash
int F4(ltree *p){
    int n=0;
    for (ltree *q=p->child; q!=NULL; q=q->next) {
        int vv=F4(q); if (vv > n) n=vv;
    }
    return n+1;
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F4</i> рекурсивно находит максимальную глубину дерева. Для каждого дочернего узла проверяется глубина, и наибольшая глубина увеличивается на 1.
</div>

# //------------------------------------------------------- 5
```bash
void F5(ltree *p, int l, int &m) {
    if (l > m) m=l;
    for (ltree *q=p->child; q!=NULL; q=q->next)
    F5(q,l+1,m);
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F5</i> рекурсивно находит максимальную глубину дерева, начиная с корня. Глубина увеличивается на 1 для каждого уровня.
</div>

# //------------------------------------------------------- 6
```bash
int F6(ltree *p, int l) {
    int n=l;
    for (ltree *q=p->child; q!=NULL; q=q->next)
    n+=F6(q,l+1);
    return n;
}
double main6(ltree *p) {
    return ((double)F6(p,1))/F1(p);
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F6</i> рекурсивно подсчитывает общее количество узлов в дереве, начиная с корня. Результат делится на общее количество узлов для получения среднего значения.
</div>

# //------------------------------------------------------- 7
```bash
int F7(ltree *p) {
    if (p->val > 6) return p->val;
    for (ltree *q=p->child; q!=NULL; q=q->next) {
        int vv=F7(q); if (vv!=-1) return vv;
    }
    return -1;
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F7</i> рекурсивно ищет в дереве узел, значение которого больше 6. Если такой узел найден, возвращается его значение.
</div>

# //------------------------------------------------------- 8
```bash
int F8(btree *p) {
    if (p==NULL) return 0;
    return (1 + F8(p->r) + F8(p->l));
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F8</i> рекурсивно подсчитывает количество узлов в бинарном дереве. Для каждого поддерева возвращается 1, и результаты складываются.
</div>

# //------------------------------------------------------- 9
```bash
int F9(btree *p) {
    if (p==NULL) return 0;
    int nr=F9(p->r);
    int nl=F9(p->l);
    return 1 + (nr>nl ? nr : nl);
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F9</i> рекурсивно находит глубину бинарного дерева, выбирая максимальную глубину между левым и правым поддеревьями.
</div>

# //------------------------------------------------------- 10
```bash
int F10(btree *p) {
    if (p==NULL) return 0;
    int m,n=p->val;
    if ((m=F10(p->r))>n) n=m;
    if ((m=F10(p->l))>n) n=m;
    return n;
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F10</i> рекурсивно находит максимальное значение в бинарном дереве, проверяя каждое поддерево и выбирая наибольшее значение.
</div>

# //------------------------------------------------------- 11
```bash
int F11(tree1 *p) {
    int s=1;
    for (int i=0;i < p->n; i++) s+=F11(p->ch[i]);
    return s;
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F11</i> рекурсивно подсчитывает количество узлов в дереве с фиксированным числом детей (до 10). Для каждого дочернего узла возвращается количество узлов.
</div>

# //------------------------------------------------------- 12
```bash
int F12(tree1 *p) {
    int s=p->val;
    for (int i=0;i < p->n; i++) s+=F12(p->ch[i]);
    return s;
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F12</i> рекурсивно суммирует значения всех узлов в дереве с фиксированным числом детей. Для каждого дочернего узла добавляется значение узла к общей сумме.
</div>

# //------------------------------------------------------- 13
```bash
int F13(tree1 *p) {
    int s=p->val;
    for (int i=0;i < p->n; i++) {
        int vv=F13(p->ch[i]); 
        if (vv > s) s=vv;
    }
    return s;
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F13</i> рекурсивно находит максимальное значение в дереве с фиксированным числом детей. Для каждого дочернего узла проверяется, какое значение больше.
</div>

# //------------------------------------------------------- 14
```bash
int F14(tree1 *p) {
    int s=0;
    for (int i=0;i < p->n; i++) {
        int vv=F14(p->ch[i]);
        if (vv > s) s=vv;
    }
    return s+1;
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F14</i> рекурсивно находит максимальную глубину дерева с фиксированным числом детей. Результат увеличивается на 1 для каждого уровня.
</div>

# //------------------------------------------------------- 15
```bash
void F15(tree1 *p, int l, int &m) {
    if (l > m) m=l;
    for (int i=0;i < p->n; i++) F15(p->ch[i],l+1,m);
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F15</i> рекурсивно находит максимальную глубину дерева с фиксированным числом детей, начиная с корня.
</div>

# //------------------------------------------------------- 16
```bash
int F16(tree1 *p, int l) {
    int s=l;
    for (int i=0;i < p->n; i++)
    s+=F16(p->ch[i],l+1);
    return s;
}
double main16(tree1 *p) {
    return ((double)F16(p,1))/F11(p);
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F16</i> рекурсивно подсчитывает количество узлов в дереве с фиксированным числом детей. Результат делится на общее количество узлов для получения среднего значения.
</div>

# //------------------------------------------------------- 17
```bash
int F17(tree1 *p) {
    if (p->val > 6) return p->val;
    for (int i=0;i < p->n; i++) {
        int vv=F17(p->ch[i]);
        if (vv!=-1) return vv;
    }
    return -1;
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F17</i> рекурсивно ищет в дереве узел, значение которого больше 6. Если такой узел найден, возвращается его значение.
</div>

# //------------------------------------------------------- 18
```bash

```
<div>
<h>Ответ на 18 вопрос</h>
<p></p>
</div>

# //------------------------------------------------------- 19
```bash

```
<div>
<h>Ответ на 19 вопрос</h>
<p></p>
</div>

# //------------------------------------------------------- 20
```bash

```
<div>
<h>Ответ на 20 вопрос</h>
<p></p>
</div>

# //------------------------------------------------------- 21
```bash
int F21(tree *p) {
    if (p==NULL) return 0;
    int s=1;
    for (int i=0; i<4; i++) s+=F21(p->ch[i]);
    return s;
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F21</i> рекурсивно подсчитывает количество узлов в дереве с фиксированным числом детей (4). Для каждого дочернего узла возвращается количество узлов.
</div>

# //------------------------------------------------------- 22
```bash
int F22(tree *p) {
    if (p==NULL) return 0;
    int s=p->val;
    for (int i=0; i<4; i++) s+=F22(p->ch[i]);
    return s;
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F22</i> рекурсивно суммирует значения всех узлов в дереве с фиксированным числом детей. Для каждого дочернего узла добавляется значение узла к общей сумме.
</div>

# //------------------------------------------------------- 23
```bash
int F23(tree *p) {
    if (p==NULL) return 0;
    int s=p->val;
    for (int i=0; i<4; i++) {
        int vv=F23(p->ch[i]);
        if (vv > s) s=vv;
    }
    return s;
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F23</i> рекурсивно находит максимальное значение в дереве с фиксированным числом детей. Для каждого дочернего узла проверяется, какое значение больше.
</div>

# //------------------------------------------------------- 24
```bash
int F24(tree *p){
    if (p==NULL) return 0;
    int s=0;
    for (int i=0; i<4; i++) {
        int vv=F24(p->ch[i]);
        if (vv > s) s=vv;
    }
    return s+1; 
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F24</i> рекурсивно находит максимальную глубину дерева с фиксированным числом детей. Результат увеличивается на 1 для каждого уровня.
</div>

# //------------------------------------------------------- 25
```bash
void F25(tree *p, int l, int &m) {
    if (p==NULL) return;
    if (l > m) m=l;
    for (int i=0; i<4; i++) F25(p->ch[i],l+1,m);
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F25</i> рекурсивно находит максимальную глубину дерева с фиксированным числом детей, начиная с корня.
</div>>

# //------------------------------------------------------- 26
```bash
int F26(tree *p, int l) {
    if (p==NULL) return 0;
    int s=l;
    for (int i=0; i<4; i++) s+=F26(p->ch[i],l+1);
    return s;
}
double main26(tree *p) {
    return ((double)F26(p,1))/F21(p);
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F26</i> рекурсивно подсчитывает количество узлов в дереве с фиксированным числом детей. Результат делится на общее количество узлов для получения среднего значения.
</div>

# //------------------------------------------------------- 27
```bash
int F27(tree *p) {
    if (p->val > 6) return p->val;
    for (int i=0; i<4; i++) {
        int vv=F27(p->ch[i]); 
        if (vv!=-1) return vv;
    }
    return -1;
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F27</i> рекурсивно ищет в дереве узел, значение которого больше 6. Если такой узел найден, возвращается его значение.
</div>

# //------------------------------------------------------- 28
```bash

```
<div>
<h>Ответ на 28 вопрос</h>
<p></p>
</div>

# //------------------------------------------------------- 29
```bash

```
<div>
<h>Ответ на 29 вопрос</h>
<p></p>
</div>

# //------------------------------------------------------- 30
```bash
int F30(int A[],int sz, int n) {
    if (n>=sz || A[n]==-1) return 0;
    return 1+F30(A,sz,2*n)+F30(A,sz,2*n+1);
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F30</i> рекурсивно подсчитывает количество элементов в бинарном дереве, начиная с указанного индекса. При этом учитываются узлы, у которых значение не равно -1, и для каждого элемента считаются его левый и правый потомки.
</div>

# //------------------------------------------------------- 31
```bash
int F31(int A[],int sz, int n) {
    if (n>=sz || A[n]==-1) return 0;
    return A[n]+F31(A,sz,2*n)+F31(A,sz,2*n+1);
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F31</i> рекурсивно суммирует значения элементов бинарного дерева, начиная с заданного индекса. При этом для каждого элемента учитываются его левый и правый потомки.
</div>

# //------------------------------------------------------- 32
```bash
int F32(int A[],int sz, int n) {
    if (n>=sz || A[n]==-1) return 0;
    int xx,vv=A[n];
    if ((xx=F32(A,sz,2*n))>vv) vv=xx;
    if ((xx=F32(A,sz,2*n+1))>vv) vv=xx;
    return vv;
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F32</i> рекурсивно находит максимальное значение в бинарном дереве, начиная с указанного индекса. Для каждого элемента дерево проверяется, какое из значений (левое или правое поддерево) является наибольшим.
</div>

# //------------------------------------------------------- 33
```bash
int F33(int A[],int sz, int n) {
    if (n>=sz || A[n]==-1) return 0;
    int x1=F33(A,sz,2*n);
    int x2=F33(A,sz,2*n+1);
    return x1>x2 ? x1+1 : x2+1;
}
```
<div>
  <b>Что делает код:</b><br>
  Функция <i>F33</i> рекурсивно находит глубину бинарного дерева. Для каждого узла вычисляется максимальная глубина из левого и правого поддеревья, и результат увеличивается на 1.
</div>

